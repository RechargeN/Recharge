name: api_contracts
description: Recharge API contracts and generated client package.
version: 0.3.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  crypto: 3.0.7
  lints: ^5.0.0
  test: ^1.25.8
